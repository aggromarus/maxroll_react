/*
import React, {Component} from "react";
import {BrowserRouter, Route} from "react-router-dom";
import {NavLink} from "react-router-dom";
import {SomeComponent} from "./Comp2";
import styles from '/another_app/src/test/React.module.css';

const React = (props
) => {
    return (
        <BrowserRouter>
            <div className={styles.wrapper}>
                <NavLink to='/pageName' activeClassName ={styles.activeLink}> Ссылка в HTML </NavLink>
                <Route path="/pageName" component={Component}></Route>
                <Route path="/pageName" render={ () => <Component firstProps ={propsAnyFirst} secondProps={propsAnySecond} />}></Route>
                <Route exact path="/pageName" component={Component}></Route>
            </div>
            <SomeComponent props={123}></SomeComponent>

        </BrowserRouter>
    )
}
*/
